<%# views/pages/500.ejs %>
<div class="container text-center py-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <i class="fas fa-meteor fa-5x text-danger mb-4 animate-fall"></i>
            <h1 class="display-1 fw-bold text-danger">500</h1>
            <h2 class="mb-4">Gangguan Kosmik Server</h2>
            <p class="lead text-muted mb-4">
                Houston, kita ada masalah! Server kami sepertinya terkena hujan meteor. Tim teknisi bintang kami sedang memperbaikinya.
            </p>
            <p class="text-muted mb-4">
                Silakan coba lagi setelah badai meteor berlalu atau kembali ke stasiun utama.
            </p>

            <% if (process.env.NODE_ENV !== 'production' && typeof error !== 'undefined' && error) { %>
                <div class="alert alert-danger text-start mt-4">
                    <h5 class="alert-heading">Detail Kesalahan (Mode Pengembangan):</h5>
                    <pre style="white-space: pre-wrap; word-wrap: break-word; color: var(--danger-color); background-color: rgba(248, 113, 113, 0.1); padding: 10px; border-radius: var(--border-radius);"><%= error %></pre>
                </div>
            <% } %>

            <a href="/" class="btn btn-primary btn-lg btn-glow">
                <i class="fas fa-home me-2"></i>Kembali ke Stasiun Utama
            </a>
            <button onclick="window.location.reload();" class="btn btn-outline-secondary btn-lg ms-2">
                <i class="fas fa-sync-alt me-2"></i>Coba Lagi
            </button>
        </div>
    </div>
</div>
<style>
    @keyframes fall {
        0% { transform: translateY(-100px) rotate(0deg); opacity: 0; }
        50% { opacity: 1; }
        100% { transform: translateY(0px) rotate(15deg); opacity: 1; }
    }
    .animate-fall {
        animation: fall 1.5s ease-out forwards;
    }
     .fa-meteor {
        text-shadow: 0 0 15px var(--danger-color);
    }
</style>