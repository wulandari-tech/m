<%# views/pages/500.ejs %>
<div class="container text-center py-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <i class="fas fa-server fa-5x text-danger mb-4"></i>
            <h1 class="display-1 fw-bold">500</h1>
            <h2 class="mb-4">Terjadi Kesalahan Server</h2>
            <p class="lead text-muted mb-4">
                Maaf, terjadi kesalahan internal pada server kami. Tim kami telah diberitahu dan sedang berupaya memperbaikinya.
            </p>
            <p class="text-muted mb-4">
                Silakan coba lagi nanti atau kembali ke halaman utama.
            </p>

            <% if (process.env.NODE_ENV !== 'production' && typeof error !== 'undefined' && error) { %>
                <div class="alert alert-danger text-start mt-4">
                    <h5 class="alert-heading">Detail Kesalahan (Mode Pengembangan):</h5>
                    <pre style="white-space: pre-wrap; word-wrap: break-word;"><%= error %></pre>
                </div>
            <% } %>

            <a href="/" class="btn btn-primary btn-lg">
                <i class="fas fa-home me-2"></i>Kembali ke Beranda
            </a>
            <button onclick="window.location.reload();" class="btn btn-outline-secondary btn-lg ms-2">
                <i class="fas fa-sync-alt me-2"></i>Coba Lagi
            </button>
        </div>
    </div>
</div>