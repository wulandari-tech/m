<h1><i class="fas fa-file-invoice-dollar"></i> Status Pembelian/Sewa SC</h1>
<hr>
<div class="alert alert-<%= alertType %>" role="alert">
    <%= message %>
</div>

<% if (order_id && order_id !== 'CLOSED') { %>
    <p>Order ID Transaksi Anda: <strong><%= order_id %></strong></p>
    <p>Silakan periksa halaman "Pesanan Saya" untuk detail.</p>
<% } else if (order_id === 'CLOSED') { %>
    <p>Anda menutup jendela pembayaran sebelum menyelesaikan transaksi.</p>
<% } %>

<div class="mt-3">
    <a href="/sc-list" class="btn btn-primary"><i class="fas fa-shopping-cart"></i> Lihat SC Lain</a>
    <a href="/dashboard/orders" class="btn btn-info"><i class="fas fa-history"></i> Pesanan Saya</a>
    <a href="/dashboard" class="btn btn-secondary"><i class="fas fa-tachometer-alt"></i> Kembali ke Dashboard</a>
</div>